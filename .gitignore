# This repo is a static site with no build step and no dependencies. The previous
# .gitignore was a Python/Django template inherited from the old project and covered
# nothing that exists here.

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# editors / tooling
.vscode/
.idea/
*.swp

# local dev server config
.claude/

# nothing should ever install here, but guard against it
node_modules/
