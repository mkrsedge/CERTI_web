{
  "name": "certi-website",
  "version": "1.0.0",
  "private": true,
  "description": "getcerti.com — hand-built static site. No framework and no build step.",
  "scripts": {
    "build": "echo \"Static site: nothing to build. out/ is committed as-is.\"",
    "dev": "npx --yes http-server -p 4180 -c-1 out"
  }
}
