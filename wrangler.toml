# Cloudflare Pages project binding. Kept deliberately: this is the deployment contract,
# not old-site content. It declares the output directory, which is what lets the existing
# Pages configuration keep working untouched after the redesign.
name = "certi-website"

[pages]
pages_build_output_dir = "out"
